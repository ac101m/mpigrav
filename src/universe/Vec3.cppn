#include <Vec3.hpp>


#include <sstream>


// Get the vector as a string (v)
template<class T>
std::string Vec3<T>::Str(void) {
  std::stringstream ss;
  ss << "(" << this->x << "," << this->y << "," << this->z << ")";
  return ss.str();
}
